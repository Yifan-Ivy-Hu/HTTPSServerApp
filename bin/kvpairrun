#!/bin/bash
#
# kvpairrun
#  
#

set -x
set -Eeuo pipefail

./bin/kvpairdb reset

export FLASK_DEBUG=True
export FLASK_APP=kvpair
export KVPAIR_SETTINGS=config.py

flask run --host 0.0.0.0 --port 5050